# Commits that `git blame` should skip (pure formatting / mechanical rewrites,
# no logic change). One full 40-char commit hash per line; `#` starts a comment.
#
# Enable locally (per clone, not automatic):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# One-time `cargo fmt --all` reformat of apps/desktop/native (no logic change).
a4356f154a895aa4b3d6f219aa12bf24f8d9e43f
