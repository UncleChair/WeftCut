target/
target-*/
gen/
WixTools/

# Vendored binary deps (libmpv-2.dll is ~110 MB; regenerate via the install
# steps in docs/setup.md). Headers and the generated mpv.lib live alongside.
# Per-vendor download scripts ARE committed so contributors can regenerate.
vendor/*
!vendor/chrome-headless-shell/
vendor/chrome-headless-shell/*
!vendor/chrome-headless-shell/download.ps1
