// isPausedUnderTsActor has been removed; routing is covered by mcpRouter.test.ts and server.flip.test.ts.
import { describe, it } from 'vitest'
describe('mutationTools', () => {
  it('isPausedUnderTsActor removed — routing covered by mcpRouter.test.ts + server.flip.test.ts', () => {})
})
