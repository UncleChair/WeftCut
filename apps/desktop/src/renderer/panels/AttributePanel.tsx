// Stable semantic boundary for the contextual Attribute tool. The field
// implementation remains in properties/ while the legacy RightPanel and the
// future Dock Workspace both compose this Panel-level module.

export {
  AttributePanel,
  type AttributePanelProps,
} from "../properties/PropertyPanel";
